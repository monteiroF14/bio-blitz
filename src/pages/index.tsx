import { type NextPage } from "next";
import QuestModal from "~/components/QuestModal";

const Home: NextPage = () => {
  return (
    <main>
      <QuestModal />
    </main>
  );
};

export default Home;
