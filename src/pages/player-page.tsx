import React from "react";

const PlayerPage = () => {
  return (
    <div>
      {/* <h1 className="text-2xl font-bold">Hello, {player.name}</h1>
      <div>
        <LevelingSystemData
          currentLevel={currentLevel}
          requiredXP={requiredXP}
          currentXP={currentXP}
          questsDone={questsDone}
        />
        <p>{title}</p>
      </div>
      <PlayerRewards rewards={player.rewards} /> */}
    </div>
  );
};

export default PlayerPage;
